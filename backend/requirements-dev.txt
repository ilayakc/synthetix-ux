# Yalnizca yerel gelistirme/dogrulama (scripts/verify.ps1) icin kurulur;
# runtime Docker image'ina (backend/Dockerfile) dahil edilmez.
-r requirements.txt
pytest-cov==6.0.0
freezegun==1.5.1
ruff==0.8.6
mypy==1.14.1
