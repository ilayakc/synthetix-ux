fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
arq==0.26.3
argon2-cffi==23.1.0
pyjwt==2.10.1
# analyzer servisini cagirmak icin (bkz. app.services.page_analysis)
httpx==0.28.1
# yuklenen tasarim ekran goruntulerini decode/dogrula/EXIF-temizle/yeniden-encode
# etmek icin (bkz. app.services.design_assets)
Pillow==11.1.0
# FastAPI'nin multipart/form-data (dosya yukleme) istekerini ayristirmasi icin
python-multipart==0.0.20
# DesignAsset ekran goruntuleri icin yerel/deterministik gorsel analiz (CTA
# adaylari, sentetik dikkat tahmini) - bkz. app.services.image_visual_analysis.
# Harici vision API cagrisi yapilmaz, gorseller ucuncu taraflara gonderilmez.
numpy==2.1.3
opencv-python-headless==4.10.0.84
# OpenAI Responses API provider adaptoru (bkz. app.services.ai_pipeline.openai_provider).
# Yalnizca resmi `openai` SDK; Agents SDK veya baska bir OpenAI paketi eklenmez.
openai==2.52.1

# --- test ---
pytest==8.3.4
pytest-asyncio==0.25.2
